import '../imports/api/tasks.js';

// Meteor.startup(function() {  
//   if (Posts.find().count() === 0) {
//     for (i = 1; i <= 10; i++) {
//       Posts.insert({ text: "Task " + Random.id(), createdAt: new Date() });
//     }
//   }
// });

// Meteor.publish('posts', function() {  
//   return Posts.find();
// });
